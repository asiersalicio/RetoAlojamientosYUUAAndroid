<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/constraintSearchItem"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/searchResultIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_location_city_black_24dp" />

    <TextView
        android:id="@+id/searchResultText"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:text="Bilbao"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/searchResultIcon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.25" />

    <TextView
        android:id="@+id/searchResultTextDesc"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="La capital del mundo"
        android:textColor="#4D000000"
        android:textStyle="italic"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.45"
        app:layout_constraintStart_toStartOf="@+id/searchResultText"
        app:layout_constraintTop_toBottomOf="@+id/searchResultText"
        app:layout_constraintVertical_bias="0.25" />

    <ImageView
        android:id="@+id/searchResultIconDesc"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="4dp"
        app:layout_constraintBottom_toBottomOf="@+id/searchResultTextDesc"
        app:layout_constraintEnd_toStartOf="@+id/searchResultTextDesc"
        app:layout_constraintTop_toTopOf="@+id/searchResultTextDesc"
        app:srcCompat="@drawable/ic_location_city_black_24dp" />
</androidx.constraintlayout.widget.ConstraintLayout>